services:
  - type: web
    name: disney-antro-j-village
    runtime: static
    branch: main
    buildCommand: mkdir -p dist && cp index.html app.js styles.css dist/
    staticPublishPath: ./dist
    autoDeployTrigger: commit
